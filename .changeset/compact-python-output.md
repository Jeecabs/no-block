---
"@jeecabs/no-block": minor
---

Add compact Python output inside the existing Bash tool, including virtualenv interpreter paths such as `.venv/bin/python`. Show a short result or traceback preview. Expansion shows inline source, the original command, output, and log paths.

Keep command execution, foreground wait, cancellation, timeout, and background notifications unchanged. Unsupported or compound commands retain the native Bash view.
