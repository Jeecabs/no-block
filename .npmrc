package-lock=false
; pi installs git packages with `npm install --omit=dev`. npm 10's resolver
; crashes ("Cannot read properties of null (reading 'edgesOut')") while walking
; vitest 4's optional peer set, which leaves the package with no node_modules.
; legacy-peer-deps skips that walk. pnpm ignores this setting.
legacy-peer-deps=true
